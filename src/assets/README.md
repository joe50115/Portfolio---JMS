# assets/

Real images (screenshots, photos, concept art) go here once you have them,
imported directly into components — e.g. `import cover from "../assets/my-project-cover.png"`.

Until then, project galleries and cards use the generated placeholder
graphics from `utils/placeholder.js`.
